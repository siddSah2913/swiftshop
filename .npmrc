# Project-scoped npm script policy.
# This machine's user ~/.npmrc sets `allow-scripts=opencode-ai`, meaning npm blocks
# install scripts for every other package (secure-by-default). We allow scripts ONLY
# for the packages this project genuinely needs to run theirs (per the plan §4 stack):
allow-scripts=sharp,prisma,@prisma/engines,@prisma/client